# Keep the build context small and the image clean.
.venv/
venv/
env/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

.git/
.env
.env.*
!.env.example

# Keep the build context small, but let the two prebuilt artifacts the image
# needs through: the chunk list (Step 2 indexing) and the eval results (/metrics).
data/
!data/chunks.jsonl
eval/
!eval/results.json
doc/
brief.pdf
*.log
tests/
backend/ingestion/.cache/

# Scratch test page (host-only)
scratch-ui.html
