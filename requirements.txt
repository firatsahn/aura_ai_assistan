# Ingestion pipeline (Step 1) dependencies
pymupdf>=1.24.0       # PDF text extraction, page rasterization, table detection
openpyxl>=3.1.0       # XLSX reading (row-wise)
anthropic>=0.40.0     # Claude vision for scanned PDF / image docs (default provider)
google-genai>=1.0.0   # Gemini vision (alternative provider, VISION_PROVIDER=gemini)
python-dotenv>=1.0.0  # load credentials / provider config from .env

# Embedding & vector store (Step 2) dependencies
openai>=1.0.0         # text-embedding-3-small embeddings + gpt-4o-mini generation
qdrant-client>=1.10.0 # vector store client; >=1.10 for Query API (RRF fusion) + sparse IDF (Step 3b hybrid)

# Generation & API (Step 3a) dependencies
fastapi>=0.110.0          # POST /query endpoint (frontend + eval entry point)
uvicorn[standard]>=0.29.0 # ASGI server for the FastAPI app
